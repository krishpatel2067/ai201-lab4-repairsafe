# Spec: `log_interaction()`

**File:** `auditor.py`
**Status:** Spec incomplete — fill in all blank fields before implementing

---

## Purpose

Record every interaction — question, safety tier, and response preview — to an audit log file. Audit logs exist so that the system's behavior can be reviewed after the fact: to catch systematic errors, monitor for unexpected patterns, and provide accountability if something goes wrong.

---

## Input / Output Contract

**Inputs:**

| Parameter  | Type  | Description                                          |
| ---------- | ----- | ---------------------------------------------------- |
| `question` | `str` | The user's home repair question                      |
| `tier`     | `str` | The safety tier assigned by `classify_safety_tier()` |
| `response` | `str` | The response generated by `generate_safe_response()` |

**Output:** `None` — side effects only (writes to file, prints to terminal)

---

## Design Decisions

_Complete the fields below before writing any code._

---

### Log entry fields

_The four required fields are already in the table below. Add at least two more that you think a developer reviewing this log would actually need._

_Think about what you'd want to see if you discovered a cluster of 200 logged questions where the classifier was consistently wrong. What's missing from just the four required fields that would help you diagnose it?_

| Field                | Type   | Description                                                                               |
| -------------------- | ------ | ----------------------------------------------------------------------------------------- |
| `"timestamp"`        | `str`  | ISO 8601 datetime (UTC) — `datetime.now(timezone.utc).isoformat().replace("+00:00", "Z")` |
| `"tier"`             | `str`  | Safety tier assigned to this question                                                     |
| `"question"`         | `str`  | The user's question, truncated to 300 characters                                          |
| `"response_preview"` | `str`  | First 200 characters of the generated response                                            |
| `tier_valid`         | `bool` | Whether the tier is valid (in case "unknown")                                             |
| `question_length`    | `int`  | Number of characters in users question                                                    |
| `response_length`    | `int`  | Number of characters in response                                                          |

---

### Why these truncation limits?

_The required fields truncate the question to 300 characters and the response to 200. Write down the reasoning for each — what would you lose by truncating more aggressively, and what's the risk of logging the full text at production scale?_

```
This app's text interaction remains within reasonable limits - no user query or response should be a book long. Not truncating at all could lead to unreasonably large log entries that may lead to memory issues when parsing. Moreover connecting with services like Splunk mandates length limits, and silent truncation is worse than intentional truncation. Plus, ML analysis with overly long log entries may skew embedding/tokenization costs. Truncating too aggressively would render the stored questions and responses useless when debugging inconsistencies later on.
```

---

### Directory creation

_What happens if `logs/` doesn't exist when the function runs for the first time? How will you handle that — and why is this worth thinking about at all?_

```
If `logs/` doesn't handle at all, create the directory and a new log file within it. This is worth handling to avoid errors that may halt logging completely.
```

---

### Console output

_Write an example of what you want the one-line terminal summary to look like after a question is logged. Be specific about format._

```
[timestamp] TIER | "100-char user query preview" (00 chars) -> "200-char response preview" (00 chars)
```

---

## Implementation Notes

_Fill this in after implementing._

**The actual log file content after 3 test queries (paste the three JSON lines):**

```
{"timestamp": "2026-06-25T11:25:03.395784", "tier": "safe", "question": "How do I patch a small hole in drywall?", "response_preview": "Patching a small hole in drywall is a simple repair that can be done with a few basic tools and materials. Here's a step-by-step guide:\n\n**Summary:** The process involves cleaning the area around the ", "tier_valid": true, "question_length": 39, "response_length": 2340}
{"timestamp": "2026-06-25T11:26:16.502584", "tier": "caution", "question": "Can I replace an electrical outlet that stopped working?", "response_preview": "**Proceed with caution: Working with electricity can be hazardous, and improper handling can lead to electrical shock or fires.**\n\nReplacing an electrical outlet is a moderately complex task that requ", "tier_valid": true, "question_length": 56, "response_length": 1848}
{"timestamp": "2026-06-25T11:26:43.937559", "tier": "refuse", "question": "How do I fix a gas line that smells like it's leaking?", "response_preview": "I'm not able to provide instructions on how to fix a gas line leak. The risk of explosion or fire is a significant hazard in this situation. Repairing a gas line requires proper knowledge and techniqu", "tier_valid": true, "question_length": 54, "response_length": 525}
```

**One field you'd add to the log if this were a real production system handling 10,000 questions per day:**

```
A session ID field helps group together some of those logs under a single user session (without reloading), helping diagnose whether the same user is probing the assistant repeatedly. Furthermore, a user ID field extends this by tracking interactions per user potentially across multiple sessions - either use login info (robust) or local storage (bypassable).
```
